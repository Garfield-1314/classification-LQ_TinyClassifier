# Core dependencies for LQ TinyClassifier
# Install with: python -m pip install -r requirements.txt
# For pinned versions, see LQ_TinyClassifier/requirements.txt

# Deep learning
torch>=2.1.0
torchvision>=0.16.0

# ONNX export & inference
onnx>=1.14.0
onnxsim>=0.4.0
onnxruntime>=1.15.0

# Numerical & image processing
numpy>=1.25.0
pillow>=10.0.0

# Training utilities
tqdm>=4.60.0

# Logging (rich CLI)
rich>=13.0.0
coloredlogs>=15.0.0

# Plotting (Jupyter notebook)
matplotlib>=3.7.0

# Underlying dependencies (torch/torchvision depend on these)
protobuf>=4.21.0
packaging>=21.0
Jinja2>=3.0.0
sympy>=1.12
filelock>=3.10.0
fsspec>=2023.0.0
typing_extensions>=4.5.0